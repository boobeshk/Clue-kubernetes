apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: clue-test
  labels:
    app: doc-1
    app.kubernetes.io/name: doc-1
  name: doc-1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-fix-demo
  template:
    metadata:
      labels:
        app: ai-fix-demo
        app.kubernetes.io/name: doc-1
        app.kubernetes.io/managed-by: clue
    spec:
      containers:
        - name: demo
          image: nginx:1.25
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: true
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          startupProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 30
          imagePullPolicy: IfNotPresent
---
apiVersion: v1
kind: Service
metadata:
  name: ai-fix-demo
  namespace: clue-test
  labels:
    app: ai-fix-demo
    app.kubernetes.io/name: ai-fix-demo
    app.kubernetes.io/managed-by: clue
spec:
  selector:
    app: ai-fix-demo
    app.kubernetes.io/name: doc-1
    app.kubernetes.io/managed-by: clue
  ports:
    - port: 80
      targetPort: 80
