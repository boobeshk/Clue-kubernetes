apiVersion: v1
kind: Namespace
metadata:
  name: autofix-demo
  namespace: default
  labels:
    app: autofix-demo



---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-no-memory-limit
  namespace: autofix-demo
  labels:
    app: test-no-memory-limit
spec:
  replicas: 2
  selector:
    matchLabels:
      app: test-no-memory-limit
  template:
    metadata:
      labels:
        app: test-no-memory-limit
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
